# This file was auto-generated by the Firebase CLI
# https://github.com/firebase/firebase-tools

name: Deploy to Firebase Hosting on PR
on: pull_request
permissions:
  checks: write
  contents: read
  pull-requests: write
jobs:
  build_and_preview:
    if: ${{ github.event.pull_request.head.repo.full_name == github.repository }}
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      # --- DEBUG STEP ADDED ---
      - name: List Repository Contents for Debug
        run: ls -laR
      # ------------------------

      - name: Run npm install and build
        run: npm install && npm run build
        working-directory: web # This is the line we suspect is failing due to path/case error

      - uses: FirebaseExtended/action-hosting-deploy@v0
        with:
          repoToken: ${{ secrets.GITHUB_TOKEN }}
          firebaseServiceAccount: ${{ secrets.FIREBASE_SERVICE_ACCOUNT_FAMILY_TREE_9EEB8 }}
          projectId: family-tree-9eeb8
